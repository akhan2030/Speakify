// Progress tracker — band history, question types practised, and streaks.
import StudentComingSoon from "@/components/StudentComingSoon";

export default function ListeningTrackerPage() {
  return (
    <StudentComingSoon activePage="listening" skillName="Listening — Tracker" />
  );
}
